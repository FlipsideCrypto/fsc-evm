version: 2
models:
  - name: test_streamline__contract_abis_recent

    columns:
      - name: CONTRACT_ADDRESS
        tests:
          - not_null
          - dbt_expectations.expect_column_values_to_match_regex:
              regex: "^(0x)[0-9a-fA-F]{40}$"
