models:
  - name: observability__logs
    columns:
      - name: TEST_TIMESTAMP
        tests:
          - not_null
      - name: BLOCKS_IMPACTED_COUNT
        tests:
          - accepted_values:
              values: [0]
              config:
                where: "TEST_TIMESTAMP = (SELECT MAX(TEST_TIMESTAMP) FROM {{ this }})" 